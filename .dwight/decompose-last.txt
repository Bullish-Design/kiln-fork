-- DwightAuto decomposition output
-- Request: Create the documentation under docs for the backlinks that are shown in the right sidebar.
-- Timestamp: Wed 11 Mar 2026 04:39:02 PM CET

Here's the decomposition for creating backlinks documentation:

<tasks>
<task order="1" title="Create Backlinks documentation page">
Create a new documentation file at `docs/Features/User Interface/Backlinks.md` that documents the backlinks feature shown in the right sidebar.

Follow the EXACT same documentation format used by other User Interface feature docs in this project (e.g., `docs/Features/User Interface/Table of Contents.md`, `docs/Features/User Interface/Local Graph.md`, `docs/Features/User Interface/Layouts.md`). The format is:

1. YAML frontmatter with `title` and `description` fields
2. H1 heading matching the title
3. Overview paragraph explaining what backlinks are
4. Sections covering the feature

Here is the content to write:

**Frontmatter:**
- title: Backlinks
- description: Kiln automatically detects and displays backlinks in the right sidebar, showing every page in your vault that links to the current page.

**Sections to include:**

### Overview (after H1, no heading)
Explain that Kiln scans your vault for [[Wikilinks]] and standard Markdown links, then builds a reverse index. For every page, the **Backlinks** panel in the **Right Sidebar** lists all other pages that link to it. This helps readers discover related content and navigate your knowledge graph in reverse.

### Why it is useful
- In a wiki or digital garden, backlinks reveal hidden relationships between notes
- They answer "What pages reference this concept?" without manual curation
- They complement the [[Local Graph]] by providing a clickable list of incoming connections

### How it works
Explain the generation process:
1. During the build, Kiln scans every note for outgoing links (both `[[wikilinks]]` and `[markdown](links.md)`)
2. For each link found, a reverse reference is recorded on the target page
3. The result is a list of all pages that point to the current page
4. Self-links are excluded — a page cannot appear in its own backlinks
5. Duplicate links from the same source are deduplicated

### Behavior by Layout

#### Default Layout
- Backlinks appear in the **Right Sidebar**, below the [[Table of Contents]] and [[Local Graph]]
- Each backlink is a clickable link that navigates to the source page
- Navigation uses [[Client Side Navigation]] (HTMX) for instant page transitions without a full reload

#### Simple Layout
- Backlinks are accessed via a floating action button in the header bar
- Clicking the button opens a panel overlay listing all incoming links
- The button only appears when the current page has at least one backlink

### Disabling Backlinks
If your site does not need backlinks, you can hide them using the `--disable-backlinks` flag:

```bash
kiln generate --disable-backlinks
```

Or set it permanently in your `kiln.yaml` configuration file:

```yaml
disable-backlinks: true
```

When disabled, the backlinks panel is hidden from both the default and simple layouts. See [[Configuration File]] for all available options.

**Cross-references to use (as wikilinks):**
- [[Wikilinks]]
- [[Local Graph]]
- [[Table of Contents]]
- [[Client Side Navigation]]
- [[Configuration File]]
- [[Global Graph]]

**Verify:** Check that the file exists at `docs/Features/User Interface/Backlinks.md` and has valid YAML frontmatter.
</task>
<task order="2" title="Update generate command docs and layouts docs with backlinks references" depends_on="1">
Update two existing documentation files to reference the new Backlinks page:

**File 1: `docs/Commands/generate.md`**

In the Flags table (around line 28-41), add a new row for the `--disable-backlinks` flag. It should be inserted after the `--disable-local-graph` row (line 40). The new row should be:

```
| `--disable-backlinks`   |       | `false`   | Hides the [[Backlinks]] panel from the right sidebar.                                                                    |
```

Also update the `--disable-local-graph` description on line 40. The current text says "Disabling both TOC and local graph removes the right sidebar entirely." Change it to say "Disabling TOC, local graph, and backlinks removes the right sidebar entirely."

**File 2: `docs/Features/User Interface/Layouts.md`**

In the "Layout Details" section:

1. Under "### Default (Obsidian Parity)" (around line 33-38), the Right Sidebar bullet currently says:
   `- **Right Sidebar:** Contains the **[[Table of Contents]]** and **[[Local Graph]]**.`
   Change it to:
   `- **Right Sidebar:** Contains the **[[Table of Contents]]**, **[[Local Graph]]**, and **[[Backlinks]]**.`

2. Under "### Simple (Minimalist)" (around line 40-44), the "Action Buttons" bullet currently says:
   `- **Action Buttons:** Tools like the Graph, Table of Contents, and Search are tucked away behind floating buttons or a simplified header.`
   Change it to:
   `- **Action Buttons:** Tools like the Graph, Table of Contents, Backlinks, and Search are tucked away behind floating buttons or a simplified header.`

**Verify:** Read the edited files and confirm the backlinks references are present and properly formatted.
</task>
</tasks>
