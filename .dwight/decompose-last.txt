-- DwightAuto decomposition output
-- Request: Under docs you can find 3 empty files: "Feed RSS.md", "Image Optimization.md" and "Link Preview.md". Read the other files in the Features folder and create for these empty files the missing documentat
-- Timestamp: Tue 10 Mar 2026 03:17:23 PM CET

Now I have a thorough understanding of the documentation style and the technical details for all three features. Let me produce the task decomposition.

<tasks>
<task order="1" title="Write Feed RSS documentation">
Write the documentation file `docs/Features/Feed RSS.md`. 

Read the existing documentation files in `docs/Features/` to match the style exactly. All docs use YAML frontmatter with `title` and `description` fields, an H1 heading matching the title, an explanatory opening paragraph, then H2 sections. They use wikilinks (`[[Page Name]]`) for cross-references, code blocks with language identifiers, callout blocks (`> [!warning]`), bold for key terms, and tables where appropriate.

**Source code context for accuracy:**
- RSS implementation lives in `internal/rss/rss.go` and `internal/obsidian/rss.go`
- During vault scanning, every `.md` file is automatically processed for RSS inclusion
- Metadata extracted per file: title (from frontmatter, falls back to filename), description (from frontmatter), WebPath, PubDate (file creation/birth time)
- Entries are sorted newest-first and limited to 50 most recent
- Output file is `feed.xml` in the output directory
- RSS 2.0 compliant XML format
- Channel title and link use the site's BaseURL
- The `--url` flag is required for absolute URLs in the feed; without it, RSS generation is skipped
- No explicit opt-in needed — all `.md` files are included automatically
- No way to exclude specific files or customize max entries

**Document structure to follow:**

```
---
title: "Feed RSS"
description: "..."
---
# Feed RSS

Opening paragraph explaining what RSS is and that Kiln auto-generates a feed.

## Why it matters
Explain RSS benefits for readers (subscribe, aggregators, etc.)

## Configuration
Explain the --url flag requirement (similar to Sitemap xml.md pattern).
Include a bash code block showing the command.
Add a callout warning about missing --url flag.

## How it works
- All .md files included automatically
- Title from frontmatter `title` field, falls back to filename
- Description from frontmatter `description` field (optional)
- Publication date from file creation time
- Sorted newest first, max 50 entries
- Output: `feed.xml` in root of output directory

## Customizing entries
Explain how frontmatter fields affect RSS output. Show a YAML frontmatter example.

## Limitations
- Max 50 entries (hard-coded)
- All .md files included, no way to exclude specific files
- No full-text content in feed (title + description only)
- Publication date uses file creation time, cannot override via frontmatter
- Channel title uses BaseURL, not a custom site name
```

Use wikilinks to cross-reference related pages like `[[Sitemap xml]]`, `[[Configuration File]]`. Keep the tone instructional and clear, matching the existing docs. Do NOT add any `@feature:` pragma — this is a markdown documentation file, not source code.

**Verify:** Read the file after writing to confirm it has proper frontmatter and is non-empty.
</task>

<task order="2" title="Write Image Optimization documentation">
Write the documentation file `docs/Features/Image Optimization.md`.

Read the existing documentation files in `docs/Features/` to match the style exactly. All docs use YAML frontmatter with `title` and `description` fields, an H1 heading matching the title, an explanatory opening paragraph, then H2 sections. They use wikilinks (`[[Page Name]]`) for cross-references, code blocks with language identifiers, callout blocks (`> [!tip]`, `> [!warning]`), bold for key terms, and tables where appropriate.

**Source code context for accuracy:**
- Implementation in `internal/imgopt/imgopt.go` and wired in `internal/builder/builder_default.go`
- Responsive image rendering in `internal/obsidian/markdown/wikilinks.go`
- Supported input formats: `.png`, `.jpg`, `.jpeg`
- Output formats: AVIF (best compression), WebP (good compression), original format (fallback)
- Default breakpoints: 1200px, 800px, 400px wide
- Quality: 80 (hard-coded)
- Requires external CLI tools: `cwebp` (from libwebp) for WebP, `avifenc` (from libavif) for AVIF
- If tools are missing, graceful degradation — originals are copied as-is, no error
- Each optimizable image produces up to 9 variants (3 sizes × 3 formats)
- HTML output uses `<picture>` element with `<source>` tags for each format and `srcset`/`sizes` attributes
- All images get `loading="lazy"` attribute
- Images also get an expand button (lightbox) for full-size viewing
- No user configuration needed — runs automatically at build time for supported formats
- GIF, SVG, WebP inputs are NOT optimized (copied as-is)

**Document structure to follow:**

```
---
title: "Image Optimization"
description: "..."
---
# Image Optimization

Opening paragraph: Kiln automatically optimizes images at build time, generating responsive variants in modern formats.

## How it works
Explain the pipeline: decode → resize at breakpoints → encode to AVIF/WebP/original → generate <picture> HTML.
Mention breakpoints (1200, 800, 400), quality (80), and format priority.

## Supported formats
Table showing input formats (.png, .jpg, .jpeg) and what they produce.
Note that .gif, .svg, .webp are copied as-is without optimization.

## Requirements
Explain the two external tools needed:
- `cwebp` from libwebp (for WebP)
- `avifenc` from libavif (for AVIF)
Include install commands for common systems (apt, brew, nix).
Add a callout: if tools aren't installed, Kiln silently falls back to copying originals.

## The generated HTML
Show the <picture> element structure with <source> tags. Explain srcset, sizes, and lazy loading.
Mention the expand/lightbox button.

## Usage
Explain that no configuration is needed. Just reference images normally in markdown:
- `![alt](image.png)` or `![[image.png]]`
Kiln handles the rest.

## Limitations
- External tools required for WebP/AVIF
- Fixed breakpoints and quality (not configurable)
- GIF/SVG not optimized
- Increases output size (multiple variants per image)
- Adds build time proportional to image count/size
```

Use wikilinks to cross-reference `[[Wikilinks]]` for image embedding syntax. Keep the tone instructional. Do NOT add any `@feature:` pragma.

**Verify:** Read the file after writing to confirm it has proper frontmatter and is non-empty.
</task>

<task order="3" title="Write Link Preview documentation">
Write the documentation file `docs/Features/Link Preview.md`.

Read the existing documentation files in `docs/Features/` to match the style exactly. All docs use YAML frontmatter with `title` and `description` fields, an H1 heading matching the title, an explanatory opening paragraph, then H2 sections. They use wikilinks (`[[Page Name]]`) for cross-references, code blocks, callout blocks, bold for key terms, and tables where appropriate.

**Source code context for accuracy:**
- Implementation in `assets/link_preview.js` (client-side JavaScript)
- Styling in `assets/shared.css` (tooltip CSS with theme variable support)
- Works only on desktop: requires viewport ≥1024px AND hover-capable device (`matchMedia("(hover: hover)")`)
- Targets all `a.internal-link` elements (class added automatically by Kiln's markdown renderer for wikilinks)
- Hover flow: 300ms delay → fetch target page HTML → parse and extract `#content .content` → render in tooltip
- Tooltip positioning: prefers below link, falls above if insufficient space, constrained to viewport
- Max tooltip size: 400px wide × 300px tall, scrollable if content overflows
- In-memory cache (Map) persists for browser session — each page fetched only once
- Tooltip inherits `.content` styles for visual consistency with page content
- Tooltip respects light/dark theme via CSS variables
- Automatically dismissed on navigation (htmx:beforeSwap), scroll, Escape key, or clicking outside
- Fully automatic — no opt-in or configuration needed
- Does NOT work for: external links, hash-only links (#), current page links, links with target="_blank"
- No keyboard/accessibility support
- Vanilla JavaScript, no external libraries

**Document structure to follow:**

```
---
title: "Link Preview"
description: "..."
---
# Link Preview

Opening paragraph: Kiln shows a live preview tooltip when you hover over internal links, letting readers peek at content without navigating away.

## How it works
Explain the hover → delay → fetch → render flow.
Mention caching (fetched once per session).
Mention tooltip positioning (below or above link).

## Compatibility
- Desktop only (≥1024px viewport)
- Hover-capable devices (not touch-only)
- Modern browsers with JavaScript enabled
Add a callout noting that on mobile/tablet the feature is automatically disabled — readers simply follow links normally.

## What triggers a preview
Explain which links get previews:
- Internal wikilinks (rendered with class `internal-link`)
- NOT external links, hash links, or links to the current page

## Visual behavior
- Tooltip appears after a short delay (300ms)
- Max 400×300px, scrollable
- Styled consistently with page content
- Follows light/dark theme automatically
- Dismissed by: moving mouse away, scrolling, pressing Escape, or navigating

## Limitations
- Desktop only — disabled on mobile and touch devices
- No keyboard navigation or screen reader support
- Downloads full page HTML to extract content (no dedicated preview endpoint)
- Cache grows unbounded during session
- No loading indicator during fetch
- No error feedback if fetch fails
```

Use wikilinks to cross-reference `[[Wikilinks]]`, `[[Light-Dark Mode]]`, `[[Client Side Navigation]]`. Keep the tone instructional. Do NOT add any `@feature:` pragma.

**Verify:** Read the file after writing to confirm it has proper frontmatter and is non-empty.
</task>
</tasks>
