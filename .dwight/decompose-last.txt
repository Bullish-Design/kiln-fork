-- DwightAuto decomposition output
-- Request: Read the features under docs and create a new file for the accent color that documents how the accent color works.
-- Timestamp: Tue 10 Mar 2026 06:44:16 PM CET

Now I have a thorough understanding of the accent color feature and the documentation style. This is a documentation-only task — creating a single new Markdown file. It's straightforward enough that it doesn't need decomposition into multiple sub-tasks; however, the developer asked for decomposition, so I'll structure it minimally.

<tasks>
<task order="1" title="Research accent color implementation across codebase">
Read the following files to understand how the accent color feature works end-to-end. Take detailed notes on what you find — you will use them in the next task to write documentation.

Files to read:
1. `internal/config/config.go` — look at the `AccentColor` field in the `Config` struct and the `ValueOr` method's `"accent-color"` case
2. `internal/cli/commands.go` — look at `DefaultAccentColor`, `FlagAccentColor`, `FlagAccentColorShort`, and the `accentColor` variable
3. `internal/cli/generate.go` — look at how `accentColor` flag is registered (description, short flag `-a`), applied via `applyStringFlag`, and wired to `builder.AccentColorName`
4. `internal/builder/builder.go` — look at the `AccentColorName` package variable
5. `internal/builder/themes.go` — look at `ResolveTheme` (how it calls `overrideAccentColor`), `paletteColor` (the switch with valid color names: red, orange, yellow, green, blue, purple, cyan), and `overrideAccentColor` (how it replaces `theme.Light.Accent` and `theme.Dark.Accent`)
6. `internal/templates/helpers.go` — look at `writeColorVars` and how `--accent-color` CSS variable is set from `c.Accent`
7. `kiln.yaml` — see the commented-out `# accent-color: blue` example
8. `assets/shared.css` — see how `var(--accent-color)` is used in CSS (scrollbar hover, etc.)

Also read these existing doc files to understand the documentation style/format used (frontmatter with title/description, heading structure, tables, configuration examples with bash code blocks, references to other docs via `[[wikilinks]]`):
- `docs/Features/User Interface/Themes.md`
- `docs/Features/User Interface/Fonts.md`
- `docs/Features/I18n.md`

Summarize all findings — do NOT write any files yet.
</task>
<task order="2" title="Write Accent Color documentation file" depends_on="1">
Create a new documentation file at `docs/Features/User Interface/Accent Color.md`.

Follow the exact style and format of the existing docs at `docs/Features/User Interface/Themes.md`, `docs/Features/User Interface/Fonts.md`, and `docs/Features/I18n.md`. The format uses:
- YAML frontmatter with `title` and `description` fields
- A top-level `#` heading matching the title
- Sections with `##` subheadings
- Tables for structured data
- Bash code blocks for CLI examples
- YAML code blocks for config file examples
- `[[wikilinks]]` to reference related docs (like `[[generate]]`, `[[Configuration File]]`, `[[Themes]]`)

The document should cover these sections:

**1. Intro paragraph** — Explain that by default, each theme comes with its own built-in accent color (used for links, active states, scrollbar hovers, and other interactive elements). The accent color feature lets you override this default with any color from the theme's own palette, keeping visual harmony while personalizing your site.

**2. How it works** — Explain the mechanics:
- Each theme defines a palette of 7 named colors: red, orange, yellow, green, blue, purple, cyan
- When you set an accent color, Kiln replaces the theme's default accent in both light and dark mode variants with the palette color matching that name
- The override happens at build time — the chosen color is baked directly into the CSS custom property `--accent-color`
- If the color name is not recognized, Kiln logs a warning and keeps the theme's original accent

**3. Available colors** — A table listing the 7 valid color names:

| Name | Description |
|:-----|:------------|
| `red` | Red from the theme palette |
| `orange` | Orange from the theme palette |
| `yellow` | Yellow from the theme palette |
| `green` | Green from the theme palette |
| `blue` | Blue from the theme palette |
| `purple` | Purple from the theme palette |
| `cyan` | Cyan from the theme palette |

Add a note that the actual hex values depend on the chosen [[Themes|theme]] — for example, `red` in Dracula is `#ff5555` while in Nord it's `#bf616a`.

**4. Configuration** — Show how to set it:
- CLI flag: `kiln generate --accent-color blue` (short form `-a`)
- Config file (`kiln.yaml`): `accent-color: blue`
- Reference that CLI flags override the [[Configuration File]]
- Show a combined example: `kiln generate --theme dracula --accent-color cyan`

**5. Default behavior** — One short paragraph: if no accent color is specified, each theme uses its own built-in accent color (e.g., `#7e6df7` for the default theme, `#ff79c6` for Dracula).

This file does NOT get a `@feature:` pragma — it is a Markdown documentation file in the docs vault, not a source code file.

After writing the file, verify it exists and has the correct structure by reading it back.
</task>
</tasks>
