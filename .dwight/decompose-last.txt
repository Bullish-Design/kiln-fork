-- DwightAuto decomposition output
-- Request: Avif optimization doesn't work. Avif optimization should have precedence over webp.
-- Timestamp: Sun 08 Mar 2026 04:57:52 PM CET

Good. The builders call `ProcessImage` and store results in `ImageResults` maps. The fix in `ProcessImage` to add AVIF generation (and ordering it before WebP) is the correct and complete fix — it propagates through the existing `writePicture` rendering correctly.

Now I have everything I need to produce the task decomposition. The changes are actually quite straightforward — the developer has already written the correct code in their working copy. The task is small enough that it's really just 2 sub-tasks:

<tasks>
<task order="1" title="Add AVIF generation and ordering tests">
Fix AVIF optimization in the imgopt package. The committed `ProcessImage` function in `internal/imgopt/imgopt.go` only generates WebP variants — it's missing AVIF entirely. AVIF must be generated with higher priority (before WebP) so that `<picture>` elements list AVIF sources first.

**Files to edit:**

1. `internal/imgopt/imgopt_test.go` — Add tests for AVIF generation and ordering:
   - Add `"os/exec"` to the import block.
   - In `TestProcessImage`, after the existing breakpoint width checks (after line ~116), add two new test sections:
     - **AVIF variant generation check**: When `avifenc` is on PATH (check with `exec.LookPath("avifenc")`), assert that at least 2 AVIF variants are produced and that each variant's `OutPath` file exists on disk.
     - **AVIF-before-WebP ordering check**: When both `avifenc` and `cwebp` are on PATH, find the index of the first AVIF variant and the first WebP variant in `result.Variants`. Assert that the first AVIF index is less than the first WebP index.

2. `internal/imgopt/imgopt.go` — In the `ProcessImage` function, add AVIF encoding **before** the existing WebP block. Inside the `for _, bp := range breakpoints` loop, after the original format variant is appended (after the line `WebPath: webDir + "/" + origName,` closing brace), add:
   - Build `avifName` as `baseName + suffix + ".avif"` and `avifPath` as `filepath.Join(outDir, avifName)`.
   - Call `writeEncoded(avifPath, resized, EncodeAVIF, 80)`. If no error, append a `Variant{Width: bp, Suffix: suffix, Format: "avif", OutPath: avifPath, WebPath: webDir + "/" + avifName}` to `result.Variants`.
   - This block must appear BEFORE the existing "Try WebP" block so that AVIF variants are ordered before WebP variants in the slice.

**Verify:** `go test ./internal/imgopt/ -v -run TestProcessImage` — all assertions pass. AVIF variants are generated when `avifenc` is available, and they appear before WebP variants in the result.
</task>
<task order="2" title="Add picture element AVIF ordering test" depends_on="1">
Add a test for the `<picture>` element rendering to verify AVIF `<source>` tags appear before WebP `<source>` tags.

**File to edit:** `internal/obsidian/markdown/picture_test.go`

Add a new test function `TestWriteImage_WithAVIFAndWebP` after the existing `TestWriteImage_NoVariants` function. This test should:

1. Create an `IndexResolver` with `ImageResults` containing a `/img/photo` key mapped to an `imgopt.Result` with these variants (in this exact order, matching what `ProcessImage` now produces with AVIF before WebP):
   - `{Width: 800, Format: "avif", WebPath: "/img/photo-800w.avif"}`
   - `{Width: 400, Format: "avif", WebPath: "/img/photo-400w.avif"}`
   - `{Width: 800, Format: "webp", WebPath: "/img/photo-800w.webp"}`
   - `{Width: 400, Format: "webp", WebPath: "/img/photo-400w.webp"}`
   - `{Width: 800, Format: "png", WebPath: "/img/photo-800w.png"}`
   - `{Width: 400, Format: "png", WebPath: "/img/photo-400w.png"}`

2. Call `r.writeImage(w, "/img/photo", []byte("a photo"), nil)` and flush the buffer.

3. Assert the output contains: `<picture>`, `</picture>`, `<source type="image/avif"`, `<source type="image/webp"`, `loading="lazy"`.

4. Assert AVIF source ordering: use `strings.Index` to find the position of `<source type="image/avif"` and `<source type="image/webp"` in the output. Assert that the AVIF index is less than the WebP index (AVIF has precedence).

The existing `writePicture` function in `internal/obsidian/markdown/wikilinks.go` already preserves insertion order of formats from the `Variants` slice, so no implementation changes are needed — the test should pass because task 1 ensures AVIF variants come before WebP in the `Variants` slice.

**Verify:** `go test ./internal/obsidian/markdown/ -v -run TestWriteImage` — all `TestWriteImage_*` tests pass, including the new AVIF ordering test.
</task>
</tasks>
