-- DwightAuto decomposition output
-- Request: The new image optimization feature works well and does not have any problems with webp images if `libwebp` is installed. avif support doesn't seem to work: if `libavif` is installed it does not optimi
-- Timestamp: Sun 08 Mar 2026 04:36:59 PM CET

Now I have the full picture. The bug is clear:

1. **`ProcessImage` only tries WebP** (lines 206-217) — it never calls `EncodeAVIF`, so AVIF is never generated even when `avifenc` is available.
2. **Format order in `writePicture`** uses insertion order from variants — we need AVIF `<source>` to come before WebP for browser precedence.
3. When both encoders are available, AVIF should be preferred (listed first in `<picture>` element).

<tasks>
<task order="1" title="Add AVIF encoding to ProcessImage and fix format ordering">
**Context:** The `internal/imgopt/imgopt.go` file contains `ProcessImage` (line 172) which generates responsive image variants. It currently only tries WebP encoding (lines 206-217) despite having a working `EncodeAVIF` function (lines 125-167). The user reports that AVIF images are never generated even when `libavif`/`avifenc` is installed. Additionally, when both WebP and AVIF are available, AVIF should take precedence (appear first in `<source>` elements).

**What to fix in `internal/imgopt/imgopt.go`:**

1. In `ProcessImage`, after the WebP encoding block (lines 206-217), add an equivalent AVIF encoding block that calls `writeEncoded` with `EncodeAVIF`. The AVIF block should look like:
```go
// Try AVIF.
avifName := baseName + suffix + ".avif"
avifPath := filepath.Join(outDir, avifName)
if err := writeEncoded(avifPath, resized, EncodeAVIF, 80); err == nil {
    result.Variants = append(result.Variants, Variant{
        Width:   bp,
        Suffix:  suffix,
        Format:  "avif",
        OutPath: avifPath,
        WebPath: webDir + "/" + avifName,
    })
}
```

2. **AVIF must have precedence over WebP.** The `writePicture` function in `internal/obsidian/markdown/wikilinks.go` (line 357) renders `<source>` elements in the order they appear in `formatOrder`, which is built from the order variants appear in `result.Variants`. Browsers pick the **first** `<source>` they support, so AVIF must come before WebP in the variants list. 

   To achieve this, put the AVIF encoding block **BEFORE** the WebP block in `ProcessImage`. The final order inside the breakpoint loop should be:
   - Encode original format (png/jpeg) — already there
   - Try AVIF (new — add before WebP)
   - Try WebP (already there — move after AVIF)

3. **Update the test** in `internal/imgopt/imgopt_test.go`. The existing `TestProcessImage` (line 73) only checks for PNG variants. Update or add a test that verifies:
   - When `avifenc` is on PATH, AVIF variants are generated (you can check this conditionally using `exec.LookPath("avifenc")`)
   - When both are available, AVIF variants appear before WebP variants in `result.Variants`
   - The test should still pass when neither encoder is available (skip the format-specific checks)

**Also update** `internal/obsidian/markdown/picture_test.go` — the existing `TestWriteImage_WithVariants` (line 14) only has webp+png variants. Add a test case `TestWriteImage_WithAVIFAndWebP` that provides variants in order avif, webp, png and verifies that the `<source type="image/avif"` appears before `<source type="image/webp"` in the output. This confirms the `writePicture` function respects the ordering from `ProcessImage`.

**Verify:** `go test ./internal/imgopt/ ./internal/obsidian/markdown/ -run "TestProcessImage|TestWriteImage|TestIsOptimizable" -v`

**Pragma:** All edited files already have their `@feature:imgopt` or `@feature:wikilinks` pragmas — do not remove them.
</task>
</tasks>
