-- DwightAuto decomposition output
-- Request: The "Back to the top" button should appear inside the `<main>` tag. Then implement localization: a user could decide the language of the site, this adds the lang attribute in the html tag and changes 
-- Timestamp: Tue 10 Mar 2026 02:23:37 PM CET



I have all the context I need. Let me finish the decomposition concisely.

<tasks>
<task order="1" title="Move BackToTop button visually inside main">
The BackToTop button is already inside `<main>` in the DOM (line 113 of `layout_default.templ`, line 67 of `layout_simple.templ`), but CSS `position: fixed` makes it overlap the right sidebar on desktop. Fix this so the button appears visually within the main content area.

**Files to edit:**

1. **`internal/templates/layout_default.templ`** (line 92): Add `layout-default` class to `<main>`:
   - Change: `<main class="flex-1 flex flex-col h-dvh overflow-hidden" id="kiln-main">`
   - To: `<main class="flex-1 flex flex-col h-dvh overflow-hidden layout-default" id="kiln-main">`

2. **`assets/shared.css`** (after line 1253, end of `.back-to-top` rules): Add a media query so the button shifts left when the right sidebar is visible (288px = w-72):
   ```css
   @media (min-width: 1280px) {
     .layout-default .back-to-top {
       right: calc(288px + 1.5rem);
     }
   }
   ```

3. **Regenerate templ**: Run `templ generate` from project root.

**Verify**: `go build ./...` and `go test ./...`
</task>

<task order="2" title="Add lang CLI flag and config plumbing" depends_on="1">
Add a `lang` option flowing through CLI → config → builder → template types. Default: `"en"`.

**Files to edit:**

1. **`internal/cli/commands.go`**: Add `DefaultLang = "en"` to defaults block (after line 31). Add `FlagLang = "lang"` and `FlagLangShort = "g"` to flag name block (after line 58). Add `lang string` to var block (after line 75).

2. **`internal/cli/generate.go`**: In `init()`, register: `cmdGenerate.Flags().StringVarP(&lang, FlagLang, FlagLangShort, DefaultLang, "Language code for the site (e.g. en, it, fr)")`. In `runGenerate()`, add `applyStringFlag(cmd, FlagLang, &lang, cfg, DefaultLang)` and `builder.Lang = lang`.

3. **`internal/config/config.go`**: Add `Lang string \`yaml:"lang"\`` to Config struct. Add `case "lang": val = c.Lang` to `ValueOr` switch.

4. **`internal/builder/builder.go`**: Add `Lang string` to the var block.

5. **`internal/templates/types.go`**: Add `Lang string` to `SiteData` struct.

6. **`internal/builder/builder_default.go`**: Add `Lang string` to `DefaultSite` struct (~line 863). In `buildDefault()` where DefaultSite is initialized (~line 66), add `Lang: Lang,`.

7. **`internal/builder/templ_adapter.go`**: In `toTemplPageData()`, add `Lang: p.Site.Lang,` to the SiteData block (~line 37).

8. **`internal/templates/layout_default.templ`** (line 6): Change `<html lang="en">` to `<html lang={ data.Site.Lang }>`.

9. **`internal/templates/layout_simple.templ`** (line 6): Change `<html lang="en">` to `<html lang={ data.Site.Lang }>`.

10. **Regenerate templ**: Run `templ generate`.

**Verify**: `go build ./...` and `go test ./...`
</task>

<task order="3" title="Create i18n package with label registry" depends_on="2">
Create `internal/i18n/` package with a `Labels` struct and language registry.

**Create `internal/i18n/i18n.go`** — Line 1: `// @feature:layouts Localization label registry for UI strings.`

Define:
```go
type Labels struct {
    SearchPlaceholder string // "Search notes..."
    ToggleTheme       string // "Toggle theme"
    BackToTop         string // "Back to top"
    OnThisPage        string // "On this page"
    TableOfContents   string // "Table of contents"
    LocalGraph        string // "Local Graph"
    GeneratedWith     string // "Generated with"
    PageNotFound      string // "Page not found"
    GoBackHome        string // "Go back home"
    Folder            string // "Folder:"
    Tag               string // "Tag:"
    Updated           string // "Updated"
    Created           string // "Created"
    Words             string // "words"
    MinRead           string // "%d min read" (fmt format)
    Copy              string // "Copy"
    NoResults         string // "No results found"
    Search            string // "Search"
    Navbar            string // "Navbar"
    Expand            string // "Expand"
    LastModified      string // "Last Modified"
}
```

Create `var languages = map[string]*Labels{...}` with `"en"` and `"it"` entries filled with appropriate translations.

Create `func Resolve(lang string) *Labels` — returns labels for the given code, falls back to `"en"`.

**Create `internal/i18n/i18n_test.go`** — Line 1: `// @feature:layouts Tests for localization label registry.`

Tests:
- `TestResolve_English`: Resolve("en") returns correct English labels
- `TestResolve_Italian`: Resolve("it") returns Italian labels
- `TestResolve_UnknownFallback`: Resolve("xx") returns English labels
- `TestResolve_AllFieldsPopulated`: Use `reflect` to check no field is empty string for each supported language

**Verify**: `go test ./internal/i18n/...`
</task>

<task order="4" title="Wire labels into template data and replace hardcoded strings" depends_on="3">
Pass `Labels` through template data and replace all hardcoded UI strings in `.templ` files and helpers.

**Files to edit:**

1. **`internal/templates/types.go`**: Import `"github.com/otaleghani/kiln/internal/i18n"`. Add `Labels *i18n.Labels` to `SiteData` struct (existing fields: BaseURL, SiteName, Theme, NavbarRoot, DisableLocalGraph, DisableTOC, FlatURLs, Lang).

2. **`internal/builder/templ_adapter.go`**: Import `"github.com/otaleghani/kiln/internal/i18n"`. Add `Labels: i18n.Resolve(p.Site.Lang),` to the SiteData block in `toTemplPageData()`.

3. **`internal/templates/helpers.go`**: Change `FormatReadingTime(minutes int) string` to `FormatReadingTime(minutes int, format string) string` — use `fmt.Sprintf(format, minutes)`.

4. **`internal/templates/content.templ`** — Replace hardcoded strings. For components that don't receive `data` (like `BackToTop()`, `SimpleSearchButton()`, `SimpleTOCButton()`, `SimpleLocalGraphButton()`, `SimpleMenuButton()`), change their signature to accept `labels *i18n.Labels`. Then update their call sites in `layout_default.templ` and `layout_simple.templ` to pass `data.Site.Labels`. Key replacements:
   - `BackToTop()` → `BackToTop(data.Site.Labels)`: use `labels.BackToTop` for aria-label and title
   - `NoteMetaBar`: use `data.Site.Labels.Words`, `.Updated`, `.Created`, `.MinRead` (pass format to `FormatReadingTime`)
   - `Content404`: use `data.Site.Labels.PageNotFound`, `.GoBackHome`
   - `ContentFolder`: use `data.Site.Labels.Folder`
   - `ContentTag`: use `data.Site.Labels.Tag`
   - `LocalGraph(disabled bool)` → `LocalGraph(disabled bool, labels *i18n.Labels)`: use `labels.LocalGraph`, `labels.Expand`
   - `TOCPanel`: use `data.Site.Labels.OnThisPage`
   - `SimpleSearchButton()` → `SimpleSearchButton(labels *i18n.Labels)`: use `labels.Search`
   - `SimpleTOCButton()` → `SimpleTOCButton(labels *i18n.Labels)`: use `labels.TableOfContents`
   - `SimpleLocalGraphButton()` → `SimpleLocalGraphButton(labels *i18n.Labels)`: use `labels.LocalGraph`
   - `SimpleTOCContainer`: use `data.Site.Labels.TableOfContents`
   - `SimpleMenuButton()` → `SimpleMenuButton(labels *i18n.Labels)`: use `labels.Navbar`
   - Footer text in `SimpleMenu` and `layout_default.templ`: use `data.Site.Labels.GeneratedWith`

5. **`internal/templates/shared.templ`**: `ThemeToggler()` → `ThemeToggler(labels *i18n.Labels)`: use `labels.ToggleTheme` for aria-label. Update call sites in both layout files.

6. **`internal/templates/layout_default.templ`**: Update call sites — pass `data.Site.Labels` to changed component calls. Also update the search button placeholder (line 74): change `Search notes...` to `{ data.Site.Labels.SearchPlaceholder }`. Update footer `Generated with` text.

7. **`internal/templates/layout_simple.templ`**: Update call sites similarly.

8. **Regenerate templ**: Run `templ generate`.

**Verify**: `go build ./...` and `go test ./...`
</task>

<task order="5" title="Pass labels to JavaScript for client-side strings" depends_on="4">
Some UI strings live in JavaScript files (`assets/default_app.js`, `assets/simple_app.js`, `assets/search.js`): "Copy", "No results found". Pass them via a data attribute or inline script so they're localizable.

**Files to edit:**

1. **`internal/templates/layout_default.templ`** and **`internal/templates/layout_simple.templ`**: Add a hidden element inside `<body>` that carries JS-facing labels as data attributes:
   ```html
   <div id="kiln-labels" hidden
     data-copy={ data.Site.Labels.Copy }
     data-no-results={ data.Site.Labels.NoResults }
   ></div>
   ```

2. **`assets/default_app.js`**: Where `btn.textContent = "Copy"` appears (lines 352, 360), read from `document.getElementById("kiln-labels")?.dataset.copy || "Copy"` instead.

3. **`assets/simple_app.js`**: Same change for "Copy" strings (lines 252, 260).

4. **`assets/search.js`**: Where `msg.textContent = "No results found"` (line 176), read from `document.getElementById("kiln-labels")?.dataset.noResults || "No results found"`.

5. **Regenerate templ**: Run `templ generate`.

**Verify**: `go build ./...` and `go test ./...`
</task>
</tasks>
