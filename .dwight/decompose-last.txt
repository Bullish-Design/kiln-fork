-- DwightAuto decomposition output
-- Request: There are a couple of things to change regarding the image optimization feature. First thing first the order of the `<source>` tag should be avif->webp->png. If we put png on top every browser is goin
-- Timestamp: Sun 08 Mar 2026 05:15:39 PM CET

Now I have the complete picture. The fix is straightforward — there are two approaches:

1. **Fix in `ProcessImage()`**: Reorder variant appends so AVIF comes first, then WebP, then original format
2. **Fix in `writePicture()`**: Use an explicit format order instead of relying on first-seen order from variants

The cleanest fix is approach 1 (reorder in `ProcessImage`) since the `writePicture` function already respects the order from the variants slice, and existing tests already check that AVIF appears before WebP. We just need to also ensure PNG comes last.

This is actually a small, focused change — only 2 files need editing. Let me decompose it:

<tasks>
<task order="1" title="Fix source tag order: avif → webp → png">
The `<picture>` element currently emits `<source>` tags in the wrong order. The original format (PNG/JPEG) appears first because `ProcessImage()` in `internal/imgopt/imgopt.go` appends the original-format variant before AVIF and WebP for each breakpoint. Since `writePicture()` in `internal/obsidian/markdown/wikilinks.go` emits `<source>` tags in first-seen format order, browsers pick PNG first.

**What to change:**

1. **Edit `internal/imgopt/imgopt.go`** — In the `ProcessImage()` function (lines 172-234), reorder the variant appends inside the `for _, bp := range breakpoints` loop so that AVIF is tried first, then WebP, then the original format. Currently the order is:
   - Original format (line 198-204)  
   - AVIF (lines 206-217)
   - WebP (lines 219-230)

   Change to:
   - AVIF first (try encoding, append variant if successful)
   - WebP second (try encoding, append variant if successful)
   - Original format last (always appended)

   This ensures the `Result.Variants` slice has formats in avif→webp→png order, which `writePicture()` will respect when building `formatOrder`.

2. **Edit `internal/imgopt/imgopt_test.go`** — In `TestProcessImage()` (lines 134-151), the existing test checks that "AVIF must appear before WebP" when both encoders are available. Add an additional check: when all three formats are present, PNG must appear **after** both AVIF and WebP. Specifically, after the existing AVIF-before-WebP check (line 147-149), add a check that `firstPNG` index is greater than both `firstAVIF` and `firstWebP`. Track `firstPNG` alongside the existing `firstAVIF` and `firstWebP` variables.

3. **Edit `internal/obsidian/markdown/picture_test.go`** — In `TestWriteImage_WithAVIFAndWebP()` (lines 72-117), add assertions that:
   - WebP source appears before PNG source  
   - PNG source appears last (after both AVIF and WebP)
   
   Currently it only checks AVIF before WebP (lines 108-116). Add:
   ```go
   pngIdx := strings.Index(out, `<source type="image/png"`)
   if pngIdx < 0 {
       t.Fatalf("expected png source in output:\n%s", out)
   }
   if pngIdx <= webpIdx {
       t.Errorf("PNG source (at %d) must appear after WebP source (at %d)\ngot: %s", pngIdx, webpIdx, out)
   }
   ```

   Also in `TestWriteImage_WithVariants()` (lines 13-46), add a PNG source check: the test has variants with webp then png formats. Add an assertion that the webp `<source>` appears before the png `<source>` in the output.

**Verify command:** `go test ./internal/imgopt/... ./internal/obsidian/markdown/...`
</task>
</tasks>

This is a single-task decomposition because the change is small and focused — it's just reordering variant appends in one function and updating the corresponding tests. No new files, no new types, no architectural changes needed.
