-- DwightAuto decomposition output
-- Request: - [ ] **Configuration File:** Support for `kiln.yaml` to save preferences (flags) persistently, so you don't need to pass CLI flags on every [generate](./Commands/generate.md) run.
-- Timestamp: Sun 08 Mar 2026 05:53:49 PM CET

Now I have a thorough understanding of the codebase. Let me design the decomposition.

The key observations:
1. The project uses `gopkg.in/yaml.v3` already (a dependency).
2. Configuration is currently done via CLI flags in `internal/cli/commands.go` with global vars, and the `run*` functions copy flag values into `builder.*` package-level vars.
3. The convention is package-per-domain, with `@feature:` pragmas on line 1.
4. Tests use standard `testing` package with `t.TempDir()` for filesystem tests.
5. The config file should be `kiln.yaml` and map to the same fields as CLI flags.

<tasks>
<task order="1" title="Config type and YAML loader">
Create a new package `internal/config` that defines the `Config` struct and provides functions to load it from a `kiln.yaml` file.

**Files to create:**

1. `internal/config/config_test.go` — Tests first (TDD). Add `// @feature:cli` pragma on line 1. Write the following tests using standard `testing` package with `t.TempDir()`:
   - `TestLoad_ValidFile`: Create a temp dir, write a `kiln.yaml` with all supported fields (theme, font, url, name, input, output, mode, layout, flat-urls, disable-toc, disable-local-graph, port, log), call `Load(path)`, assert each field is correctly parsed.
   - `TestLoad_PartialFile`: Write a `kiln.yaml` with only `theme` and `url` set, call `Load(path)`, assert those fields are set and all others are zero values (empty string / false).
   - `TestLoad_FileNotFound`: Call `Load` with a nonexistent path, assert it returns `nil, nil` (no file = no config, not an error — allows graceful fallback to defaults).
   - `TestLoad_InvalidYAML`: Write a file with `:::bad yaml[[[`, call `Load`, assert non-nil error.
   - `TestLoad_EmptyFile`: Write an empty file, call `Load`, assert returns a zero-valued `*Config` with no error.

2. `internal/config/config.go` — Implementation. Add `// @feature:cli Configuration file loading and type definitions` pragma on line 1. Define:
   ```go
   package config

   type Config struct {
       Theme             string `yaml:"theme"`
       Font              string `yaml:"font"`
       URL               string `yaml:"url"`
       Name              string `yaml:"name"`
       Input             string `yaml:"input"`
       Output            string `yaml:"output"`
       Mode              string `yaml:"mode"`
       Layout            string `yaml:"layout"`
       FlatURLs          bool   `yaml:"flat-urls"`
       DisableTOC        bool   `yaml:"disable-toc"`
       DisableLocalGraph bool   `yaml:"disable-local-graph"`
       Port              string `yaml:"port"`
       Log               string `yaml:"log"`
   }

   // Load reads a kiln.yaml file from the given path.
   // Returns (nil, nil) if the file does not exist (not an error).
   // Returns (*Config, nil) on success.
   // Returns (nil, error) on parse failure.
   func Load(path string) (*Config, error)
   ```
   Implementation: use `os.Open`, check `os.IsNotExist` to return `(nil, nil)`, otherwise `yaml.NewDecoder(f).Decode(&cfg)` (using `gopkg.in/yaml.v3`). Handle `io.EOF` from empty files by returning a zero-value Config.

**Verify:** `go test ./internal/config/...`
</task>

<task order="2" title="Config discovery with FindFile">
Add a function to locate `kiln.yaml` in the current directory (or a given directory), and add a helper to merge config values with CLI flag defaults.

**Files to edit:**

1. `internal/config/config_test.go` — Add these additional tests (the file already has 5 tests from task 1):
   - `TestFindFile_Exists`: Create a temp dir, write `kiln.yaml` in it, call `FindFile(dir)`, assert it returns the correct path and no error.
   - `TestFindFile_NotFound`: Call `FindFile` on an empty temp dir, assert it returns `""` and no error.
   - `TestApplyIfEmpty_OnlyOverridesEmpty`: Create a `Config{Theme: "dracula", Font: ""}`, call `cfg.ValueOr("theme", "default")`, assert returns `"dracula"`. Call `cfg.ValueOr("font", "inter")`, assert returns `"inter"`. This tests that config values take precedence over defaults when set, but defaults apply when config values are empty.

2. `internal/config/config.go` — Add the following functions (file already has `Config` struct and `Load` from task 1):
   ```go
   // DefaultFilename is the conventional name for the config file.
   const DefaultFilename = "kiln.yaml"

   // FindFile looks for kiln.yaml in the given directory.
   // Returns the full path if found, or "" if not present.
   // Returns an error only for filesystem errors (not for missing file).
   func FindFile(dir string) (string, error)

   // ValueOr returns the config string field if non-empty, otherwise the fallback.
   func (c *Config) ValueOr(field, fallback string) string

   // BoolOr returns the config bool field if the config is non-nil, otherwise the fallback.
   func (c *Config) BoolOr(field string, fallback bool) bool
   ```
   - `FindFile`: use `os.Stat(filepath.Join(dir, DefaultFilename))`, return path if exists, `""` if `os.IsNotExist`.
   - `ValueOr`: switch on field name (matching yaml tag names: "theme", "font", "url", "name", "input", "output", "mode", "layout", "port", "log"), return the struct field if non-empty, else fallback.
   - `BoolOr`: same pattern for bool fields ("flat-urls", "disable-toc", "disable-local-graph").

**Verify:** `go test ./internal/config/...`
</task>

<task order="3" title="Wire config into generate command">
Integrate `kiln.yaml` loading into the `generate` CLI command so that config file values act as defaults that CLI flags can override. The precedence is: CLI flag (if explicitly set) > kiln.yaml value > hardcoded default.

**Files to edit:**

1. `internal/cli/generate.go` — This file currently defines `cmdGenerate`, registers flags in `init()`, and has `runGenerate` that copies flag vars to `builder.*` globals. Modify `runGenerate` to:
   - At the start of `runGenerate`, call `config.FindFile(".")` to discover `kiln.yaml`.
   - If found, call `config.Load(path)` to parse it. Log a debug message on success, log a warning on error (but don't abort — fall back to defaults).
   - For each flag, use Cobra's `cmd.Flags().Changed("flagname")` to check if the user explicitly set it on the command line. If the flag was NOT explicitly changed by the user, AND the config file has a non-empty value for that field, use the config value instead.
   - Implement this with a helper pattern: for string flags, `if !cmd.Flags().Changed(FlagTheme) { themeName = cfg.ValueOr("theme", DefaultThemeName) }`, and similarly for bool flags using `BoolOr`.
   - The rest of `runGenerate` (copying to `builder.*` globals and calling `builder.Build`) stays the same.
   - Add import for `"github.com/otaleghani/kiln/internal/config"`.

2. `internal/cli/generate.go` — Also add a `log` output line (using `slog`) indicating when a config file is loaded, e.g. `log.Info("Loaded configuration", "file", path)` so users get feedback.

**Do NOT edit any other command files in this task** — only `generate.go`. Other commands (serve, clean, etc.) will be wired in the next task.

**Verify:** `go build ./cmd/kiln/` (ensures it compiles). Then `go test ./...` to ensure no regressions.
</task>

<task order="4" title="Wire config into serve and clean commands">
Integrate `kiln.yaml` loading into the `serve` and `clean` commands, following the exact same pattern established in task 3 for `generate`.

**Files to edit:**

1. `internal/cli/serve.go` — This file defines `cmdServe` with flags for `port`, `output`, and `log`. Modify `runServe` to:
   - At the start, call `config.FindFile(".")` and `config.Load(path)` (same pattern as generate).
   - For each flag (`port`, `output`, `log`), check `cmd.Flags().Changed(flagName)`. If not changed, use the config value via `cfg.ValueOr(...)`.
   - Add import for `"github.com/otaleghani/kiln/internal/config"`.

2. `internal/cli/clean.go` — This file defines `cmdClean` with flags for `output` and `log`. Modify `runClean` to:
   - Same pattern: discover config, load it, apply non-overridden values.
   - Add import for `"github.com/otaleghani/kiln/internal/config"`.

3. `internal/cli/doctor.go` — This file defines `cmdDoctor` with flags for `input` and `log`. Modify `runDoctor` to:
   - Same pattern.
   - Add import for `"github.com/otaleghani/kiln/internal/config"`.

4. `internal/cli/stats.go` — This file defines `cmdStats` with flags for `input` and `log`. Modify `runStats` to:
   - Same pattern.
   - Add import for `"github.com/otaleghani/kiln/internal/config"`.

5. `internal/cli/init.go` — This file defines `cmdInit` with flags for `input` and `log`. Modify `runInit` to:
   - Same pattern.
   - Add import for `"github.com/otaleghani/kiln/internal/config"`.

For all commands, the logic is: if `FindFile` returns a path, load the config. If loading fails, log a warning and continue with defaults. For each flag, only apply config values when the flag was not explicitly set by the user.

**Verify:** `go build ./cmd/kiln/` and `go test ./...`
</task>

<task order="5" title="Refactor config loading into shared helper">
The config-loading boilerplate added in tasks 3 and 4 is now duplicated across 6 command files. Refactor it into a single shared helper in `internal/cli/commands.go` to keep the code DRY.

**Files to edit:**

1. `internal/cli/commands.go` — This file already has constants, flag name definitions, and global vars. Add:
   ```go
   // loadConfig discovers and loads kiln.yaml from the current directory.
   // Returns a non-nil Config (possibly zero-valued) that is safe to call methods on.
   // Logs a message when a config file is found and loaded.
   func loadConfig(cmd *cobra.Command) *config.Config
   ```
   Implementation:
   - Call `config.FindFile(".")`. If path is empty, return `&config.Config{}`.
   - Call `config.Load(path)`. If error, log warning, return `&config.Config{}`.
   - Return the loaded config.
   
   Also add a helper to apply config values for common flag patterns:
   ```go
   // applyStringFlag sets the target variable to the config value if the CLI flag was not explicitly set.
   func applyStringFlag(cmd *cobra.Command, flagName string, target *string, cfg *config.Config, defaultVal string)
   
   // applyBoolFlag sets the target variable to the config value if the CLI flag was not explicitly set.
   func applyBoolFlag(cmd *cobra.Command, flagName string, target *bool, cfg *config.Config, defaultVal bool)
   ```
   Add import for `"github.com/otaleghani/kiln/internal/config"`.

2. `internal/cli/generate.go` — Replace the inline config-loading code added in task 3 with calls to `loadConfig(cmd)`, `applyStringFlag(...)`, and `applyBoolFlag(...)`. The `runGenerate` function should become much shorter.

3. `internal/cli/serve.go` — Same refactor: replace inline config code with helper calls.

4. `internal/cli/clean.go` — Same refactor.

5. `internal/cli/doctor.go` — Same refactor.

6. `internal/cli/stats.go` — Same refactor.

7. `internal/cli/init.go` — Same refactor.

**Verify:** `go build ./cmd/kiln/` and `go test ./...`
</task>

<task order="6" title="Integration test and kiln init config scaffolding">
Add an integration test that verifies the full config file flow end-to-end, and optionally scaffold a `kiln.yaml` during `kiln init`.

**Files to create/edit:**

1. `internal/config/config_integration_test.go` — Create this file with `// @feature:cli Integration tests for config file loading` pragma. Write:
   - `TestConfigIntegration_FullPipeline`: Create a temp dir, write a `kiln.yaml` with `theme: dracula`, `font: lato`, `url: https://example.com`. Call `FindFile(dir)`, assert path found. Call `Load(path)`, assert `cfg.Theme == "dracula"`, `cfg.Font == "lato"`, `cfg.URL == "https://example.com"`. Call `cfg.ValueOr("theme", "default")`, assert returns `"dracula"` (config wins). Call `cfg.ValueOr("mode", "default")`, assert returns `"default"` (fallback wins for unset field).
   - `TestConfigIntegration_BoolFields`: Write a `kiln.yaml` with `flat-urls: true`, `disable-toc: true`. Load and verify `cfg.FlatURLs == true`, `cfg.DisableTOC == true`, `cfg.DisableLocalGraph == false`. Verify `cfg.BoolOr("flat-urls", false)` returns `true`, `cfg.BoolOr("disable-local-graph", false)` returns `false`.

2. `internal/cli/init.go` — After the vault directory and welcome note are created successfully in `runInit`, also create a minimal `kiln.yaml` in the current directory (not inside the vault). The content should be:
   ```yaml
   # Kiln configuration file
   # Uncomment and edit the options below to set defaults for your project.
   # CLI flags will override these values.
   
   # theme: default
   # font: inter
   # url: ""
   # name: "My Notes"
   # input: ./vault
   # output: ./public
   # mode: default
   # layout: default
   # flat-urls: false
   # disable-toc: false
   # disable-local-graph: false
   ```
   Use `os.WriteFile` to write it. Check if `kiln.yaml` already exists before writing (don't overwrite). Log an info message like `"Created kiln.yaml configuration file"`.

**Verify:** `go test ./internal/config/...` and `go build ./cmd/kiln/`
</task>
</tasks>
