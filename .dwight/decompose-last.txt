-- DwightAuto decomposition output
-- Request: The local graph button that makes it bigger `window.toggleGraphExpand()` is now relative to the right sidebar. It should be relative to the hole page. We then need to make the left sidebar scrollable 
-- Timestamp: Tue 10 Mar 2026 09:35:40 AM CET

Here's the decomposition into 3 sequential sub-tasks:

<tasks>
<task order="1" title="Fix graph expand to be page-relative">
The local graph expand button (`window.toggleGraphExpand()`) currently toggles the `expanded` class on `#local-graph-wrapper`, which is inside `#right-sidebar`. This causes the fixed-position overlay and expanded graph to be relative to the sidebar instead of the full page.

**Fix**: Change `toggleGraphExpand()` to toggle the `expanded` class on `document.body` instead of on `#local-graph-wrapper`. Then update the CSS selectors accordingly.

Files to edit:

1. **`assets/default_app.js`** — Lines 251-255. Change:
   ```js
   window.toggleGraphExpand = function () {
     const wrapper = document.getElementById("local-graph-wrapper");
     if (!wrapper) return;
     wrapper.classList.toggle("expanded");
   };
   ```
   To:
   ```js
   window.toggleGraphExpand = function () {
     document.body.classList.toggle("expanded");
   };
   ```

2. **`assets/default_style_input.css`** — The `.expanded header button` selector will be too broad on `body` — it would target ALL header buttons. Change it to `.expanded #local-graph-wrapper header button`. The other two selectors (`.expanded #local-graph-container` and `.expanded::before`) remain unchanged — they still work because `body.expanded` is an ancestor of `#local-graph-container`.

3. **`assets/simple_style_input.css`** — Apply the same `.expanded header button` → `.expanded #local-graph-wrapper header button` change.

4. **`assets/default_style.css`** (compiled Tailwind) — Lines 896-903. Change `.expanded header button` to `.expanded #local-graph-wrapper header button`.

Verify with: `go test ./...`
</task>
<task order="2" title="Add scrollbar styles to left sidebar" depends_on="1">
The left sidebar's scrollable navigation area uses the browser's default scrollbar. It should use the same thin scrollbar as `#content` and `#search-modal-results` in `assets/shared.css`.

The left sidebar's scrollable div is in `internal/templates/layout_default.templ` line 66: `<div class="p-6 flex-1 overflow-y-auto flex flex-col gap-2">`. It has no ID.

Files to edit:

1. **`internal/templates/layout_default.templ`** — Line 66. Add `id="left-sidebar-nav"`:
   ```
   <div id="left-sidebar-nav" class="p-6 flex-1 overflow-y-auto flex flex-col gap-2">
   ```

2. **`assets/shared.css`** — After the `#content` scrollbar styles (line ~37), add:
   ```css
   #left-sidebar-nav {
     scrollbar-width: thin;
     scrollbar-color: var(--sidebar-border) transparent;
   }
   #left-sidebar-nav::-webkit-scrollbar { width: 8px; }
   #left-sidebar-nav::-webkit-scrollbar-track { background: transparent; }
   #left-sidebar-nav::-webkit-scrollbar-thumb { background-color: var(--sidebar-border); border-radius: 4px; }
   #left-sidebar-nav::-webkit-scrollbar-thumb:hover { background-color: var(--accent-color); }
   ```

Verify with: `go test ./...`
</task>
<task order="3" title="Make right sidebar scrollable with styled scrollbar" depends_on="2">
The right sidebar content area (containing local graph + TOC) is not scrollable. Long TOCs overflow and shift the layout.

The right sidebar content div is in `internal/templates/layout_default.templ` line 136: `<div class="p-4 flex flex-col gap-8">`. It needs `overflow-y-auto`, `flex-1`, and an ID for scrollbar styling.

Files to edit:

1. **`internal/templates/layout_default.templ`** — Line 136. Change:
   ```
   <div class="p-4 flex flex-col gap-8">
   ```
   To:
   ```
   <div id="right-sidebar-content" class="p-4 flex-1 overflow-y-auto flex flex-col gap-8">
   ```

2. **`assets/shared.css`** — After the `#left-sidebar-nav` scrollbar styles (added in task 2), add:
   ```css
   #right-sidebar-content {
     scrollbar-width: thin;
     scrollbar-color: var(--sidebar-border) transparent;
   }
   #right-sidebar-content::-webkit-scrollbar { width: 8px; }
   #right-sidebar-content::-webkit-scrollbar-track { background: transparent; }
   #right-sidebar-content::-webkit-scrollbar-thumb { background-color: var(--sidebar-border); border-radius: 4px; }
   #right-sidebar-content::-webkit-scrollbar-thumb:hover { background-color: var(--accent-color); }
   ```

Verify with: `go test ./...`
</task>
</tasks>
