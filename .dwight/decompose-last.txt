-- DwightAuto decomposition output
-- Request: The Local Graph needs to be in the right sidebar, as the first thing after the header. Also we still have problems with the expanded local graph. It displays correctly in the right position, but it's 
-- Timestamp: Tue 10 Mar 2026 10:57:12 AM CET



<tasks>
<task order="1" title="Move local graph into right sidebar template">
Move the local graph component from body-level into the right sidebar, placing it as the first element after the sidebar header.

**Files to edit:**

1. **`internal/templates/layout_default.templ`**

   Current structure (lines 114-143):
   ```
   <aside id="right-sidebar">
     <header>...</header>
     <div id="right-sidebar-content">@TOCPanel</div>
   </aside>
   if !data.Site.DisableLocalGraph {
     @LocalGraph(data.Site.DisableLocalGraph)
   }
   ```

   Change to:
   - Remove lines 141-143 (the standalone `@LocalGraph` after `</aside>`)
   - Insert `if !data.Site.DisableLocalGraph { @LocalGraph(data.Site.DisableLocalGraph) }` inside the `<aside>` block, between `</header>` (line 135) and the `<div id="right-sidebar-content">` (line 136)

2. **`internal/templates/content.templ`** — Update the `LocalGraph` templ component (lines 297-334). Change the wrapper div classes from no classes to `class="p-4 border-b border-b-sidebar-border"` so it has padding and a bottom border matching the sidebar style. The `id="local-graph-wrapper"` stays the same.

**Verify:** Run `templ generate && go build ./...`
</task>

<task order="2" title="Update CSS for in-sidebar graph and fix expanded overlay" depends_on="1">
Update CSS so the local graph flows inside the sidebar normally, and fix the expanded graph being behind the overlay.

**Files to edit:**

1. **`assets/default_style_input.css`** — Replace lines 74-126 with:

   Remove these three blocks entirely:
   - `#local-graph-wrapper { position: fixed; bottom: 0; right: 0; ... }` (lines 74-82)
   - `@media (max-width: 1279px) { #local-graph-wrapper { display: none; } }` (lines 84-88)
   - `#right-sidebar.collapsed ~ #local-graph-wrapper { display: none; }` (lines 90-92)

   Keep the expanded rules (lines 94-126) but add a new rule for the wrapper in expanded state:
   ```css
   .expanded #local-graph-wrapper {
     position: fixed;
     inset: 0;
     z-index: 10000;
     padding: 0;
     border-bottom: none;
     background: transparent;
   }
   ```

   Also add `aspect-ratio: auto;` to the existing `.expanded #local-graph-container` rule so it overrides the `aspect-square` class.

2. **`assets/default_style.css`** — Apply the exact same changes (this is the compiled output). Remove lines 912-928, keep lines 929-959, add the new `.expanded #local-graph-wrapper` rule.

**Verify:** Run `go build ./...`
</task>

<task order="3" title="Update JS for expanded graph re-render and Escape key" depends_on="2">
Update JavaScript so the expanded graph re-renders at full size and can be closed with Escape.

**Files to edit:**

1. **`assets/default_app.js`** — Replace `toggleGraphExpand` (lines 251-253) with:
   ```javascript
   window.toggleGraphExpand = function () {
     var isExpanded = document.body.classList.toggle("expanded");
     if (isExpanded) {
       var handler = function(e) {
         if (e.key === "Escape") {
           document.body.classList.remove("expanded");
           document.removeEventListener("keydown", handler);
         }
       };
       document.addEventListener("keydown", handler);
     }
   };
   ```

2. **`assets/graph.js`** — After the existing `themeObserver.observe(...)` block (around line 72), add a MutationObserver on `document.body` to re-render the local graph when the `expanded` class is toggled:
   ```javascript
   const bodyClassObserver = new MutationObserver((mutations) => {
     for (const m of mutations) {
       if (m.type === "attributes" && m.attributeName === "class") {
         const local = document.getElementById("local-graph-container");
         const localWrapper = document.getElementById("local-graph-wrapper");
         if (local) {
           setTimeout(() => initGraph(null, local, localWrapper), 50);
         }
         break;
       }
     }
   });
   bodyClassObserver.observe(document.body, {
     attributes: true,
     attributeFilter: ["class"],
   });
   ```

**Verify:** Run `go build ./...`
</task>
</tasks>
